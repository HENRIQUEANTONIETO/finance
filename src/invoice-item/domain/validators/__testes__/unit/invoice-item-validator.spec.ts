import { InvoiceItemValidatorFactory } from '../../invoice-item.validator'

describe('InvoiceItem validator unit test', () => {
  it('Invalidation cases for title field', () => {
    const sut = InvoiceItemValidatorFactory.create()
    //sut.validate({})
  })
})
